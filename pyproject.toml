[project]
name = "auto-controls-app"
version = "0.1.0"
description = "Sample app for agent branch-protection experiments"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/app"]
